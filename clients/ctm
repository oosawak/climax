#!/usr/bin/env bash
set -euo pipefail

ctm_py="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)/ctm.py"

if [ -x "$ctm_py" ]; then
  exec "$ctm_py" "$@"
fi

echo "Missing executable: $ctm_py" >&2
exit 1

