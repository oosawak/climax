[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-patch-engine"
version = "0.1.0"
description = "Python patch engine for unified diff application"
requires-python = ">=3.10"

[project.scripts]
ai-patch = "ai_patch_engine.main:main"
